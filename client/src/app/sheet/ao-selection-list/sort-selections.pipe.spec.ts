import { SortSelectionsPipe } from './sort-selections.pipe';

describe('SortSelectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSelectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
