import { AttackSortPipe } from './attack-sort.pipe';

describe('AttackSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AttackSortPipe();
    expect(pipe).toBeTruthy();
  });
});
