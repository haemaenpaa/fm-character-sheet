import { ResistanceSortPipe } from './resistance-sort.pipe';

describe('ResistanceSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ResistanceSortPipe();
    expect(pipe).toBeTruthy();
  });
});
