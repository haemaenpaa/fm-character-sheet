import { ResourceSortPipe } from './resource-sort.pipe';

describe('ResourceSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceSortPipe();
    expect(pipe).toBeTruthy();
  });
});
